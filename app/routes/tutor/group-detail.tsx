import type { MetaFunction } from 'react-router'
import { GroupDetailTutorPage } from 'src/features'

export const meta: MetaFunction = () => {
  return [{ title: 'Group Detail' }, { name: '', content: 'Chi tiết nhóm' }]
}

export default GroupDetailTutorPage
