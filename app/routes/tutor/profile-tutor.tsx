import type { MetaFunction } from 'react-router'
import { ProfileTutorPage } from 'src/features'

export const meta: MetaFunction = () => {
  return [{ title: 'Profile' }, { name: '', content: 'Trang cá nhân' }]
}

export default ProfileTutorPage
